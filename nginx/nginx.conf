server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://host-gateway:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 300s;
        client_max_body_size 25M;
    }

    location /uploads/ {
        proxy_pass http://host-gateway:8000;
        proxy_set_header Host $host;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
